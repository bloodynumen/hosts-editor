<Window x:Class="hosts.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:hosts"
        mc:Ignorable="d"
        Title="hosts editor" Height="500" Width="600">
    <Grid>
        <Grid>
            <TextBox x:Name="textBox" Loaded="textBox_Loaded" Margin="0,0,0,80" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" TextWrapping="Wrap" AcceptsReturn="True" />
        </Grid>
        <Grid>
            <Button x:Name="button" Content="Save" Margin="215,0,227,33" VerticalAlignment="Bottom" Click="button_Click" MouseLeave="button_MouseLeave"/>
        </Grid>
    </Grid>
    
</Window>